# Card Game Client

This is the client-side application for the card game built with Angular.

## Setup

1. Install dependencies:
```bash
npm install
```

2. Run development server:
```bash
ng serve
```

The application will be available at `http://localhost:4200`.
